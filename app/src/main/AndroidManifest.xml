<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aainc.recyclebin">

    <!-- Permissions -->
    <!-- WRITE_EXTERNAL_STORAGE is deprecated past API 32 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <!-- API 33+ media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Optional: All files access, must request MANAGE_EXTERNAL_STORAGE at runtime -->
    <!-- <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" /> -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        android:required="false"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Override for legacy preference library -->
    <uses-sdk xmlns:tools="http://schemas.android.com/tools"
        tools:overrideLibrary="android.support.v14.preference" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/bin_app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Google Ads -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1532946298278169~3139359932"/>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- AppCompat support version meta -->
        <meta-data
            android:name="android.support.VERSION"
            android:value="26.1.0"
            tools:replace="android:value" />

        <!-- Main Activities -->
        <activity android:name="com.aainc.recyclebin.ui.Activity_LogoDisclaimer"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.aainc.recyclebin.ui.MainActivity" />

        <service
            android:name="com.aainc.recyclebin.services.ManagerService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:description="@string/bin_str_service_description"
            android:icon="@mipmap/ic_launcher" />


        <!-- Boot and restart receivers -->
        <receiver
            android:name="com.aainc.recyclebin.broadcastreceiver.RebootBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.aainc.recyclebin.broadcastreceiver.SensorRestarterBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Custom content provider -->
        <provider
            android:name="com.aainc.recyclebin.database.FilesProtectionContentProvider"
            android:authorities="com.aainc.recyclebin.contentprovider"
            android:exported="true" />

        <!-- Google Ads Activity -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
    </application>

    <!-- App-specific signature permission -->
    <permission
        android:name="com.aainc.recyclebin.permission.RECYCLE_BIN"
        android:protectionLevel="signature" />
</manifest>
